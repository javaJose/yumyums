/**
 * Created by JF.Gallegos on 3/26/2015.*/

  console.log( "tic tac toe");
var str= x;
var squares= {
    "x", "x", "x",
    "x", "x", "x",
    "x", "x", "x",// index 0-8
}
function traceX (str){
    var xTotal= 0;
    var winner= str.concat
}
for (i=0; i>winner.length; i++;){
    var letter= winner[i];
    xtotal= += squares[letter] || 0;
}
